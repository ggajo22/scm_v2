# SPEC 감사 보고서: SPEC-ORDER-026

Iteration: 3/3 (최종)
Verdict: PASS
Overall Score: 0.94

> Reasoning context ignored per M1 Context Isolation. `spec.md:21` HISTORY에 저자가 "해소했다"고 적은 서술은 **근거로 채택하지 않았다**. 1·2차 보고서는 "어떤 결함이 제기되었는가"를 확인하는 용도로만 읽었고, 판정은 현재 5개 파일 + 실제 코드베이스 재검증 + 실행 검증만을 근거로 한다. 인용·공식·수치는 문서에서 옮겨 적지 않고 전부 독립 재산출했다.

---

## 0. 감사 범위와 독립 검증 방법

읽은 파일: `spec.md`(228행), `plan.md`(267행), `acceptance.md`(737행), `spec-compact.md`(97행), `research.md`.

수행한 독립 검증:

1. **공식 재추출 후 기계 재계산** — `serializers.py:31-73`을 직접 읽어 산술과 양자화 규칙(`ROUND_HALF_UP`, 게터별 1회, `:555-615`)을 코드에서 추출한 뒤 파이썬으로 재구현했다. 그 재구현으로 **17개 AC의 기대값 + 변이표 전체(약 130개 값)를 독립 산출**했다. 문서 값은 대조용으로만 읽었다.
2. **무수정 코드 RED 시뮬레이션 전수 재실행** — 17개 AC를 하나씩 무수정 코드에 대해 시뮬레이션했다(2차의 N1이 이 방법으로만 드러났으므로 재작성된 AC-NET-016에 반드시 재적용).
3. **실행 검증 2건** — 저자의 "이 세션에서 직접 실행해 확인했다"는 주장을 **신뢰하지 않고 직접 실행**했다: `Decimal(None)` 예외, 맨 `MagicMock`의 `is None`/`__getitem__`/`.quantize()`/`== Decimal("0")`/`__truediv__`/`str()` 거동.
4. **변이 판별력 재구성** — M1·M2·M3·M4·M5·M7(협의/광의 2형태)·M8·M10과 게이트/분기/하한 삭제 변이를 코드 수준으로 구성해 **어느 AC가 실제로 깨지는지**를 픽스처 11개 × 변이별로 전수 계산했다(단독 판별자 주장 검증 목적).
5. **인용 전수 재확인** — 아래 §5에 열거. 이번 개정에서 추가·변경된 인용은 전부 실물 대조.
6. **프로그램 카운트** — REQ 정의 수/중복, 추적표 행 수, AC 헤더 수, 변이 표 행 수, 위험 행 수, `DETAIL_SERIALIZER_FIELDS` 원소 수, `test_spec_021.py` 테스트 수, `db_table` 총수, `@MX` 태그 수, fan_in, `:875-1027`에서 `_refund`를 쓰는 테스트 수.
7. **양방향 추적성 기계 검증** — 17개 AC의 `Traces:` → REQ 존재성, 25개 REQ → AC 커버리지, 추적표가 존재하지 않는 AC를 참조하는지.

---

## 1. Must-Pass 결과

- **[PASS] MP-1 REQ 번호 일관성**
  실측: `^\*\*REQ-NET-\d{3}\*\*` 정의 **25건**, `sort | uniq -d` **공집합**(중복 0). 문서 전체의 유일 `REQ-NET-\d{3}` 토큰도 **25개** — 즉 **정의되지 않은 REQ를 참조하는 곳이 0건**이다. 3자리 zero-padding 일관. 001–005(`spec.md:55,58,61,66,69`) / 010–013(`:74,77,80,83`) / 020–026(`:88,91,94,97,100,105,108`) / 030–033(`:113,116,121,124`) / 040–044(`:131,136,139,142,145`)의 **모듈 블록 번호 체계**이며 `spec.md:51`이 접두사 체계를, `§3` 모듈 제목 5개가 블록 경계를 명시한다. 블록 간 공백(006–009 등)은 우발적 누락이 아니라 문서화된 규약이며 1·2차 판정과 동일하게 PASS로 유지한다. 이번 개정에서 REQ 번호는 한 건도 변경되지 않았다.

- **[PASS] MP-2 EARS 형식 준수**
  25개 REQ 전수 확인, 5개 패턴 외 형태 **0건**. 표본 근거: Ubiquitous — `spec.md:56` "The order cost calculation ... **shall** derive every per-line-item aggregate ..."; 금지형 Ubiquitous — `:67` "The system **shall not** read `LineItem.purchase_status` ..."; Event-Driven — `:62` "**When** a line item is partially refunded ..., the system **shall** reduce ..."; State-Driven — `:70` "**While** the net book count is `0`, the system **shall** return `0` ...", `:98` "**While** an order is fully refunded ..., the seven fields **shall** return ...", `:132` "**While** an order has no `Refund` rows, all seven fields **shall** return byte-identical values ..."; Unwanted — `:89` "**If** the summed refunded quantity ... exceeds `item.quantity`, **then** the system **shall** clamp ...", `:92`; Optional — `:146` "**Where** the frontend already computes `netPaidAmount` ..., the system **shall** keep that local calculation unchanged". 1차 D10의 라벨 오기 3건은 라벨과 본문 양쪽이 교정된 상태로 유지된다(`:66`, `:97`, `:131`).

- **[PASS] MP-3 YAML frontmatter 유효성**
  `spec.md:1-11` 실측: `id: SPEC-ORDER-026`(문자열, 패턴 일치) / `version: 1.2.0`(문자열) / `status: draft`(문자열) / `created_at: 2026-08-17`(ISO 날짜) / `updated: 2026-08-18` / `author: ggajo` / `priority: High`(문자열) / `issue_number: 0` / `labels: [order, cost, margin, refund, netting, backend]`(배열). 필수 6개 필드 전부 존재·타입 일치. 형제 SPEC 기준선 대조: `SPEC-ORDER-025/spec.md:1-10`이 동일 필드 집합 + `priority: High` + `labels` 배열 ✓. `updated`(2026-08-18)와 HISTORY 최신 행 날짜(`spec.md:21` = 2026-08-18)가 일치한다(2차 N11 해소).

- **[N/A] MP-4 언어 중립성**
  단일 스택 SPEC(Django 5.x/DRF 백엔드 + 단일 React 프론트엔드, `spec.md:29-30`)이며 다중 언어 툴체인을 다루지 않는다. 자동 통과.

---

## 2. 차원별 점수 (루브릭 기준)

| 차원 | 점수 | 루브릭 밴드 | 근거 |
|------|------|-------------|------|
| Clarity | 0.90 | 1.0 밴드 − 감점 1건 | 25개 REQ 전부 단일 해석만 허용하며 구현 함정이 인라인 [HARD] 경고로 고정되어 있다(`spec.md:64`, `:119`, `:127`, `:221`). 대명사 모호성 0건. 감점: **D1**(단독 판별자 4건 주장 중 2건이 사실과 다름 — 요구사항 해석이 아니라 테스트 계약 메타데이터의 정확성 문제이며 구현 산출물을 바꾸지 않는다) |
| Completeness | 1.00 | 1.0 | HISTORY(`:15-21`) / Environment(`:25-34`) / Assumptions(`:36-45`, 6건 전부 근거+틀렸을 때 영향 병기) / Requirements(`:49-148`) / Traceability(`:152-182`) / Exclusions(`:186-201`, **10개 항목 전부 코드 인용을 동반한 구체 항목**) / 알려진 제약(`:205-213`, C1–C5) / DoD(`:217-227`) 전부 존재. frontmatter 6개 필수 필드 완비 |
| Testability | 0.90 | 1.0 밴드 − 감점 1건 | 21개 테스트 전부 문자열/정수 동등 비교 기반 이진 판정. weasel word("appropriate/adequate/reasonable/proper") **0건** — §0.1-3이 `float` 비교를, §0.1-4가 `.get()`을 명시적으로 금지한다. 기대값·변이표 **약 130개 값 전부 기계 재계산 일치, 불일치 0건**. 감점: **D1**(M7/M4 단독 판별자 주장이 재현되지 않음 — 판별력 주장의 검증 규율 결함) |
| Traceability | 0.95 | 1.0 밴드 − 감점 1건 | 기계 검증: 17개 AC 전부 `Traces:`가 **존재하는** REQ만 참조(고아 AC 0건), 추적표 25행이 존재하지 않는 AC를 참조하는 경우 0건, AC 커버리지가 없는 REQ는 **REQ-NET-041/044 두 건뿐이며 `spec.md:177`/`:180`이 "(AC 없음 — DoD 검증)"으로 위장 없이 명시**하고 `:182`의 [HARD] 무결성 규칙이 그 규약을 못 박는다. 감점: **D3**(REQ-NET-043 행이 잔차 단정을 신설한 AC-NET-015를 누락하고 N10으로 축소된 문구를 유지) |

---

## 3. 지시받은 우선순위별 검증 결과

### 3.1 [최우선] 2차 차단 결함 N1 — **RESOLVED (실질 해소, 판별력 강화 확인)**

**(i) AC-NET-016 (a)가 무수정 코드에서 진짜로 통과하는가 — 그렇다.**

현재 (a)(`acceptance.md:644-648`)는 `res_spied`의 7필드와 `res_baseline`의 7필드가 서로 동일함을 단정한다. 두 요청은 같은 픽스처·같은 URL·결정적 계산이며 요청 사이에 상태 변화가 없다. 따라서:

| 코드 상태 | baseline 7필드 | spied 7필드 | (a) |
|---|---|---|---|
| 무수정(gross) | `4453 / "51.92" / "24.27" / "2.63" / "78.82" / "37.22" / "32.08"` | 동일 | **통과** |
| 순액화 후 | `3418 / "34.69" / "18.63" / "1.93" / "55.24" / "23.52" / "29.86"` | 동일 | **통과** |

(위 두 행은 AC-NET-001 픽스처에 대해 이 감사가 독립 재계산한 값이다.) (b) `spy.call_count == 1`도 무수정 코드에서 성립한다 — `serializers.py:555-615`의 7개 게터가 전부 `_compute_cost_breakdown`(`:513-540`, `obj.pk` 키 캐시, `:531-536` 조회 3행)을 경유하고 그 안에서만 `_compute_cost_breakdown_uncached`(`:542-553`)가 1회 호출된다(코드 직접 확인). 따라서 **"무수정 코드에서 통과가 정상"이라는 6곳의 분류 문구가 이제 참이다**:

| 위치 | 문구 | 판정 |
|---|---|---|
| `acceptance.md:618` | "[HARD] 이 AC는 무수정 코드에서 통과하는 것이 정상이다" | **참** |
| `acceptance.md:684` | 추적표 "AC-NET-016 … 통과 (정상)" | **참** |
| `acceptance.md:690` | 통과 5건에 016 포함 | **참** |
| `acceptance.md:715` | "아래 5개 AC는 … 통과하는 것이 정상" | **참** |
| `plan.md:74` + `:84` | 완료 조건 "12개 AC 실패" + 표의 016 행 | **참** |
| `spec.md:220` | "AC 단위 12개 실패 / 5개 통과 … 016은 … 현재도 통과하는 것이 정상" | **참** |

`spec-compact.md:69`는 여기에 조건절("(a) 단정을 스파이 밖 baseline과의 바이트 동일성으로 구현할 때만 참이다")까지 붙여 재발 방지 장치를 남겼다.

**(ii) 바이트 동일성 형태가 `wraps` 누락과 M11을 여전히 잡는가 — 그렇다(실행 확인).**

`wraps` 누락 시(직접 실행한 결과):

```
is None: False        __getitem__: <MagicMock name='mock.__getitem__()' ...>
.quantize(): OK       == Decimal("0"): False       __truediv__/__mul__: OK
str(m['margin_usd'].quantize(...)) = "<MagicMock name='mock.__getitem__().quantize()' id='1935106460320'>"
```

즉 "7필드가 `None`이 아니다" 수준의 약한 단정으로는 **검출 불가**임이 실측으로 확인된다(`acceptance.md:654`, `plan.md:71`, R16의 전제가 사실). 반면 6개 문자열 게터는 `id=`가 박힌 repr 문자열을 반환하므로 baseline의 실제 값과 **반드시** 다르다 → (a)가 잡는다. M11(캐시 우회)에서는 게터 7개가 각각 `_compute_cost_breakdown_uncached`를 호출해 `call_count == 7`이 되어 (b)가 잡는다.

패치 대상 유효성도 재확인: `serializers.py:553`이 `return _compute_cost_breakdown_for_rate(obj, er.rate)`로 **모듈 전역 이름을 호출 시점에** 조회하므로 `mock.patch("order.serializers._compute_cost_breakdown_for_rate", ...)`가 실제로 가로챈다. 모듈 경로도 확인: `config/settings/base.py:36`에 `"order"`, `test_spec_021.py:20`이 `from order.models import ...` ✓.

**(iii) 5개 문서의 RED 게이트 수치 정합 — 전부 일치하며 값이 옳다.**

이 감사가 17개 AC를 무수정 코드에 대해 독립 시뮬레이션한 결과: **AC 단위 12 실패 / 5 통과 = 17**, **테스트 단위 16 실패 / 5 통과 = 21**. 테스트 단위 계산 근거(실패 AC별 테스트 수): 001(1)+002(1)+003(2)+004(2)+005(1)+006a(2)+006b(2)+007(1)+008(1)+011(1)+012(1)+015(1) = **16**, 통과 009/010/013/014/016 = **5**.

| 문서 | 위치 | 문구 | 판정 |
|---|---|---|---|
| `acceptance.md` | `:686-693` | AC 12/5=17, 테스트 16/5=21 표 | ✓ |
| `acceptance.md` | `:714` | RED 대상 12개 열거 | ✓ 열거 실측 12건 |
| `acceptance.md` | `:715` | 통과 5개 열거 | ✓ |
| `plan.md` | `:74`, `:76-84` | 12개 실패 + 5개 통과 표 | ✓ |
| `spec.md` | `:220` | AC 12/5, 테스트 16/5 | ✓ |
| `spec-compact.md` | `:69` | "RED 대상은 나머지 12개(테스트 단위 16 실패 / 5 통과)" | ✓ |
| `spec.md` | `:20` | HISTORY "현 17개 기준 12개" | ✓ (N1 해소로 12가 정답) |
| `research.md` | — | RED 게이트 수치 미포함 | 불일치 없음 |

2차 N4의 산술 불가 집계("12 실패 / 9 통과")는 소멸했고 어느 문서에도 남아 있지 않다.

### 3.2 [지시 2] 저자의 실행 기반 주장 — 둘 다 직접 실행해 사실로 확인

- **`Decimal(None)`** → `TypeError: conversion from NoneType to Decimal is not supported`. `plan.md:64`가 인용한 메시지와 **문자열까지 일치**한다.
- **`_make_order`** — `test_spec_021.py:69-77`을 직접 읽었다. `:74`가 `total_price=Decimal(total_price),`로 **무조건** 변환한다. 따라서 `total_price=None`을 넘기면 죽고, `Order.total_price`가 nullable(`models.py:63`)이어도 원본 헬퍼로는 **AC-NET-006b(b)(`acceptance.md:318`)를 구현할 수 없다**. `plan.md:64`의 [HARD] 지시와 위험 R17(`plan.md:162`)이 실재하는 함정을 정확히 기술한다. 나머지 헬퍼 주장도 실물 확인: `_make_line_item`(`:80-88`)은 `quantity=None` 기본값을 받지만 `purchase_status` 인자가 없고, `test_spec_023.py:866-872`의 `_refund(line_item, quantity)`는 `subtotal`/`total_tax`를 설정하지 않는다 ✓.

### 3.3 [지시 3] 이번 개정에서 추가·변경된 수치 — 전부 재계산 일치

| 항목 | 문서 값 | 감사 재계산 | 판정 |
|---|---|---|---|
| AC-NET-015 잔차 (`:597`) | `66.03 + 13.98 = 80.01`, 순매출 80.00 → `+0.01` | 동일 (`margin_usd = 66.025` → HALF_UP → `"66.03"`, `total = 13.975` → `"13.98"`) | ✓ |
| AC-NET-015 M10 하 잔차 (`:597`) | `52.80 + 27.20 = 80.00` → `0.00`, 첫 단정 깨짐 | 동일 | ✓ |
| AC-NET-005 M2 잔차 (`:260`) | `20.01` | 동일 (M2: margin `"47.12"` + total `"2.89"` = 50.01 − 30.00) | ✓ |
| AC-NET-005 잔차 (`:247`) | `27.12 + 2.89 = 30.01` → `+0.01` | ✓ |
| AC-NET-015 7필드 + M10/M1 행 (`:576-605`) | 21개 값 | 21개 전부 일치 | ✓ |
| AC-NET-001/002/004/005/006a/006b/007/008/012/013/014 기대값 + 변이표 | 약 110개 값 | 전부 일치 | ✓ |

**불일치 0건.** 1차 감사가 제시했던 `"66.02"`는 여전히 오류이고 `"66.03"`이 맞다(`Decimal("66.025")` + `ROUND_HALF_UP`) — 저자의 반박이 3회 연속 옳다.

### 3.4 [지시 4] 이번 라운드에 재배치된 판별력 주장 — 1건 성립, 1건 불성립

**(a) M6를 AC-NET-016에서 제거한 것 — 옳다.** M6의 정의(`acceptance.md:18`)는 "`Subquery`/`OuterRef` 또는 `obj.refunds.filter(...)`"이며 그 변이는 `_compute_cost_breakdown_for_rate` **내부**에 쿼리를 추가한다. 함수는 여전히 `_compute_cost_breakdown`(`serializers.py:513-540`) 캐시를 경유해 주문당 1회 호출되므로 `spy.call_count == 1`이 그대로 통과한다. 호출 수를 7로 만드는 것은 캐시 우회(M11)뿐이다. `:660`의 한계 명시와 `:695`의 `M6(009,010)`, `spec-compact.md:63`이 3곳 정합 ✓.

**(b) M7을 AC-NET-005에서 제거하고 AC-NET-012 단독으로 만든 것 — 절반만 옳다 → D1.**
AC-NET-005가 M7을 잡지 못한다는 **부분은 협의의 형태에서 옳다**(순확정 금액 0 KRW). 그러나 "M7 = AC-NET-012 **단독**"은 재현되지 않는다. 상세는 §4 D1.

### 3.5 [지시 5] 저자가 재확인했다고 주장한 카운트 — 전부 프로그램 카운트로 재확정

| 주장 | 감사 실측 방법 | 결과 |
|---|---|---|
| AC 17개 | `grep -c '^## AC-NET-'` + 블록 파싱 | **17** ✓ |
| 변이 11개 | `acceptance.md:11-23` 표 행 수 | **11**(M1–M11) ✓ |
| REQ 25개 / 추적표 25행 | 정규식 정의 수 + 표 행 수 + `uniq -d` | **25 / 25**, 중복 0 ✓ |
| 위험 17건 | `plan.md` 표 행 ID 추출 | **R1–R17, 결번 0** ✓ (`spec-compact.md:95` "17건" 일치) |
| `DETAIL_SERIALIZER_FIELDS` 45개 | 정규식 추출 + `set()` | **45개, 중복 0** ✓ (`test_spec_023.py:39-58`, 단정 위치 `:848` 실물 확인) |
| 테스트 21개 | 추적표 테스트 수 열 합계 | **21** ✓ (`test_spec_021.py`의 `^def test_` 실측도 **21** ✓) |

부수 확인: `models.py`의 `db_table` **18개** 전수 추출 → `orders_refund`가 다른 테이블명의 접두사가 아님을 확정(`acceptance.md:419`의 "단순 `in` 검사로 안전" 근거 성립). 대비 사례 `orders_line_item_note`(`models.py:306`)도 실물 확인. `serializers.py`의 `@MX:ANCHOR` **3개**(`:93`, `:346`, `:387`) = `mx.yaml:175` 한도 도달, `@MX:NOTE` **7개** → +1~2 = 8~9 ≤ `mx.yaml:177`의 10 ✓, `fan_in_anchor: 3`은 `mx.yaml:182` ✓, `_compute_cost_breakdown_for_rate` fan_in = **2**(정의 `:31`, 호출 `:279`/`:553`; `:229`/`:544`는 주석·독스트링) — 임계 미달의 정직한 기록이 사실 ✓. `test_spec_023.py:875-1027`에서 `_refund`를 실제로 쓰는 테스트는 **6건**(그 구간의 테스트 함수 8개 중 6개) → "환불 순액 표시 판정 6건"이 정확 ✓.

---

## 4. 발견된 결함

### D1. `acceptance.md:19`/`:29`/`:216`/`:260`/`:474`/`:695`/`:699` + `spec-compact.md:63`/`:65` + `spec.md:21` — **단독 판별자 4건 주장 중 M7·M4 2건이 재현되지 않는다** — Severity: **minor**

문서는 "단독 판별자는 **M4/M7/M10/M11 네 건**"(`:695`)이라 선언하고 `:699`의 [HARD] 보호 규칙을 그 위에 세운다. 각 변이를 코드 수준으로 구성해 11개 픽스처 전부에 대해 계산한 결과:

**(1) M7은 AC-NET-012 단독이 아니다 — AC-NET-001도 잡는다.**
문서가 든 예(`:502` "순확정 금액을 10.01로 먼저 반올림")인 **협의의 M7**(순확정 금액만 선반올림)을 AC-NET-001 픽스처에 적용:

| | `total_cost` | `margin_amount` |
|---|---|---|
| 정답 | `"55.24"` (34.685483+18.62810+1.926971 = 55.240554) | `"23.52"` |
| 협의 M7 | **`"55.25"`** (34.69+18.62810+1.926971 = 55.245071) | **`"23.51"`** |

두 값 모두 AC-NET-001의 기대값 표(`:82-83`)가 단정하므로 **AC-NET-001은 협의의 M7에서 실패한다**. `:474`의 근거("순확정 금액이 `10.005`로 5로 끝나는 유일한 픽스처이므로 선반올림이 여기서만 관측된다")가 틀린 이유: 선반올림은 값이 정확히 5로 끝날 때만 관측되는 것이 아니라 **반올림 오차가 합계를 센트 경계 밖으로 밀 때** 관측된다.

**(2) `:260`의 "이 픽스처에서 관측되지 않는다"는 광의의 형태에서 틀렸다.** 저자는 광의(3개 항 각각 양자화)까지 검토했다고 적었으나 `confirmed_cost`와 `total_cost`만 확인했다. `margin_amount`를 확인하지 않았다:

| AC-NET-005 | 정답 | 광의 M7 |
|---|---|---|
| `total_cost` | `"2.89"` | `"2.89"` (변화 없음 — 저자 주장 부분) |
| `margin_amount` | `"27.12"` (30 − 1.635 − 1.25 = 27.115) | **`"27.11"`** (30 − 1.64 − 1.25) |
| `margin_rate` | `"90.38"` | **`"90.37"`** |

즉 AC-NET-005는 광의의 M7을 **잡는다**(`:238-239`가 두 값을 단정). AC-NET-015도 광의에서 `margin_amount` `"66.03"` → `"66.02"`로 잡는다.

**(3) M4도 AC-NET-002 단독이 아니다 — AC-NET-012와 AC-NET-015도 잡는다.** M4(boolean 필터 복사 — 순액 > 0이면 원래 수량 전량 유지)는 **부분 환불 라인이 있는 모든 픽스처**에서 관측된다. 부분 환불 라인 보유 픽스처는 002(수량3/환불1) 외에 **012(수량2/환불1)**, **015(수량3/환불 합2 → 순액1)** 이 있다:

| 픽스처 | 정답 | M4 |
|---|---|---|
| AC-NET-012 | `total_cost "13.98"`, weight 500 | `"27.21"`, weight 1000 |
| AC-NET-015 | `total_cost "13.98"`, weight 500 | `"40.43"`, weight 1500 |

따라서 `:29`의 "AC-NET-002가 **유일한** 방어선", `:695`의 `M4(002 단독)`, `:699`의 보호 규칙 문장, `spec.md:21`의 "단독 판별자 … M4/M7/M10/M11 4건" 모두 부정확하다. **실제 단독 판별자는 M10(AC-NET-015)과 M11(AC-NET-016) 2건**이다(M10: 같은 `line_item_id`에 환불 2행인 픽스처가 015뿐 — 재확인 ✓. M11: 호출 횟수를 세는 AC가 016뿐 — 재확인 ✓).

**오류 방향과 영향 평가**: 이 결함은 **커버리지를 과대 주장하는 방향이 아니라 과소 주장하는 방향**이다. 결과적으로 (i) 어떤 변이도 미커버가 되지 않고, (ii) `spec.md:182`의 [HARD] 추적표 무결성 규칙("위반을 검출할 수 없는 AC에 매핑된 REQ는 미커버")을 위반하지 않으며 — 실제로 `:695` 커버리지 표의 **긍정 주장 11건은 전수 검증 결과 전부 참**이다 —, (iii) 보호 규칙이 실제보다 넓게 적용되어 픽스처 단순화를 더 강하게 금지할 뿐이다(그 지시 자체는 무해하고 오히려 바람직하다). 1차 D1(거짓 판별력으로 REQ가 실질 미커버) 및 2차 N1(분류 자기모순이 단독 판별자 삭제를 유도)과 달리 **구현자에게 모순된 지시를 주지 않는다**.

**권고 조치(비차단)**: `:474`의 근거 문장을 "선반올림 오차가 합계의 센트 경계를 넘길 때 관측된다"로 정정하고, `:695`를 `M7(001,012)` / `M4(002,012,015)`로, `:699`의 단독 판별자를 **M10/M11 2건**으로 축소한다. `:216`/`:260`은 "협의의 M7(순확정 선반올림)은 관측되지 않으나 광의(항목별 양자화)는 `margin_amount`가 `"27.11"`로 갈려 잡힌다"로 정확화한다. `:699`의 픽스처 보존 지시 4줄은 그대로 두는 것이 좋다.

### D2. `acceptance.md:607` — M10 하에서 갈리는 필드 수가 6개가 아니라 **7개** — Severity: **minor**

"7필드 중 **6개가 동시에 갈린다**"고 적었으나 재계산 결과 7필드 전부가 갈린다: weight 500→1000, confirmed `"10.00"`→`"20.00"`, **shipping `"2.73"`→`"5.45"`**, korea `"1.25"`→`"1.75"`, total `"13.98"`→`"27.20"`, margin `"66.03"`→`"52.80"`, rate `"82.53"`→`"66.00"`. `:601-604`의 변이표가 `shipping_cost` 열을 누락한 데서 비롯한 과소 진술이다. 판별력에는 영향이 없다(어느 쪽이든 `total_weight_grams` 단독으로 실패).

### D3. `spec.md:179` — REQ-NET-043 추적표 행이 2차 N5/N10 정정을 물려받지 못했다 — Severity: **minor**

행 내용: "AC-NET-003 (오차 0 픽스처 2건 정확 일치) + **AC-NET-005** (잔차 `0.01` 정확 일치 — **상한 자체를 검증**, 감사 D12)". 두 가지가 stale이다: (i) 이번 개정으로 **AC-NET-015도** 동일한 잔차 단정을 신설했으므로(`acceptance.md:588-595`) 그 AC가 누락되었고, (ii) `acceptance.md:256`은 N10에 따라 "상한을 일반적으로 검증하는 것이 아니다"로 축소했는데 이 행은 "상한 자체를 검증"을 유지한다. 커버리지 과소 기재이므로 무결성 규칙 위반은 아니다.

### D4. `acceptance.md:163` — 잔차 직접 단정 AC를 005만으로 서술 — Severity: **minor**

같은 문장이 잔차 `0.01` 픽스처를 005와 015 **두 건으로 정확히 열거**한 직후 "**AC-NET-005는** 정확 일치 대신 잔차 `0.01` 자체를 직접 단정한다"로 이어진다. 015도 `:590-595`에서 동일 단정을 하므로 불완전한 서술이다(거짓은 아니다). D3와 함께 한 줄로 정정 가능.

---

## 5. 인용 전수 재검증 결과 — 어긋난 인용 0건

이번 개정에서 추가·변경된 인용과 판정에 사용된 인용을 전부 실물 대조했다. **불일치 0건.**

- `serializers.py`: `:15-17`(비용 상수 3개) `:31`(함수 정의) `:36-43` `:41-46`(게이트) `:48`(`total_price or "0"`) `:50-52` `:54-59`(권수 0 분기) `:62-63` `:157-213`(`_derive_line_item_states`) `:165-178`("the cancellation signal lives in the Refund rows") `:179-184`(**`+=` 누적 실물 — REQ-NET-002의 "동일한 구성" 주장 성립**) `:186-191`(**boolean 필터 `> 0` 실물 — REQ-NET-003 경고 성립**) `:190`(조인 키 `shopify_line_item_id`) `:266-288` `:279` `:283-284`(목록 매출 0 게이트) `:463-464` `:466-498`(독립 메모이즈 + 독스트링 `:472-480`) `:513-540` `:524-530` `:531-536` `:542-553` `:553`(모듈 전역 호출) `:555-615`(게터 7개) `:570-571`(상세 매출 0 게이트) `:591-596` `:606-615`
- `models.py`: `:63`(`total_price` nullable) `:159`(`("order_cancelled", "주문취소")`) `:170`(`shopify_line_item_id` **non-null** — `line_item_id is None` 스킵이 "설계상 관측 불가"라는 `:373`의 공개가 옳음을 확정) `:176`(`quantity` nullable) `:306` `:323-342`(Refund) `:330-331`(`subtotal`/`total_tax` nullable) `:335`(`db_table`) `:336-341`(분할 환불 주석) `:342`(`unique_together`) `:516`
- `views.py`: `:59`(상세 `refunds` prefetch) `:159-160`(`page_size = 50`) `:269`(목록 `refunds` prefetch)
- `purchase_order_views.py`: `:334-342`(`Subquery`/`OuterRef`) `:337`(조인 키) `:368`(`max(...,0)`)
- `OrderDetailPage.tsx`: `:159-162`(**필터 없는 `reduce` — REQ-NET-011의 "byte-for-byte the same population" 성립**) `:163`(`netPaidAmount`) `:491-504`("상품 정보 없음") `:511-513`("결제 정보")
- `test_spec_021.py`: `:20` `:23` `:24` `:30`(정규식) `:48`(`ORDER_DETAIL_QUERY_COUNT = 8`) `:62-66` `:69-77` **`:74`** `:80-88` `:92-96` `:104-115`(T1) `:112-115`(**단정 4개 — D8 정정 유지**) `:385-395`(T14 `confirmed_cost "30.00"`) `:429`(`"1.25"`) `:433`(`total_cost "13.98"`)
- `test_spec_023.py`: `:34`(`TOTAL_QUERY_COUNT = 8`) `:39-58`(45개) `:638`/`:680-681` `:848` `:866-872` `:875-1027`
- `mx.yaml`: `:175`(`anchor_per_file: 3`) `:177`(`note_per_file: 10`) `:182`(`fan_in_anchor: 3`)
- 선행/형제 SPEC: `SPEC-ORDER-021/spec.md:222`(AC-COST-009 (c) — `orders_exchangerate` 정확히 1개) `SPEC-ORDER-023/spec.md:162`(REQ-OLIST-021) `:279`(AC-OLIST-021 절대상수 8) `SPEC-ORDER-025/spec.md:1-10`(frontmatter 기준선)

---

## 6. Chain-of-Verification Pass (2차 자기비판)

1차 통과 후 "저자의 정정 문구를 읽고 넘긴 곳이 없는가", 특히 "**정정 자체가 새 오류를 심지 않았는가**"를 기준으로 재검토했다.

- **1차 통과에서 놓쳤다 → D1.** 1차 통과에서는 2차 보고서 N2의 지적("AC-NET-005는 M7을 잡지 못한다")이 반영되었는지만 확인하고 통과시켰다. 2차 검토에서 **M7과 M4를 실제 변이 코드로 구성해 11개 픽스처 전부에 적용**하자 (i) 협의의 M7이 AC-NET-001의 `total_cost`를 `"55.25"`로 밀어 그 AC도 M7을 잡는다는 점, (ii) 광의의 M7이 AC-NET-005의 `margin_amount`를 `"27.11"`로 만든다는 점(저자와 2차 감사가 모두 `total_cost`만 보고 지나친 축), (iii) M4가 부분 환불 픽스처 3개 전부에서 관측된다는 점이 드러났다. 즉 **N2의 해소 자체가 반대 방향으로 과교정**되었다. 이 결함은 "표에 적힌 매핑을 믿지 않고 각 변이를 전 픽스처에 적용"해야만 드러난다.
- **1차 통과에서 놓쳤다 → D2.** 1차에서는 AC-NET-015의 M10 행 값 6개가 맞는지만 대조했다. 2차에서 **7필드를 전부 나열**하자 표가 `shipping_cost` 열을 빼고 본문이 "6개"라고 적은 것이 드러났다.
- **1차 통과에서 놓쳤다 → D3/D4.** 1차에서는 `acceptance.md` 쪽 N5/N10 정정만 확인했다. 2차에서 **같은 정정이 `spec.md` 추적표까지 전파되었는지**를 교차 확인하자 `:179`가 stale임이 드러났다.

재확인한 섹션(스킵 없이 다시 읽음): `spec.md` §2 가정 6건 · §3 25개 REQ 본문 전문 · §4 추적표 25행 · §5 Exclusions 10항 · §6 제약 5건 · §7 DoD 9항 / `acceptance.md` §0 + §0.1 4항 + AC 17개 전문 + 추적표 17행 + 커버리지 절 + DoD 전문 / `plan.md` §1.1 의사코드 · §2 M1–M5 · §3 영향 파일 8행 · §4 위험 17건 · §5 mx_plan · §6 검증 명령 · §7 완료 후 기록 / `spec-compact.md` 전문 / `research.md` §9, §13.

추가로 확인한 무결점 항목:

- **`plan.md` §1.1 의사코드가 실제로 정답을 낸다** — 이 감사는 문서의 기대값을 옮겨 적지 않고 **`plan.md:20-47`의 의사코드를 그대로 구현**해 17개 AC의 기대값을 산출했다. 약 130개 값이 전부 문서와 일치했다. 즉 계획의 산술 지침은 구현 가능하며 정확하다(`+=` 누적, 사전 게이트, 양측 `max(·,0)`, `obj.refunds.all()`만 사용, `:49-73` 무변경 선언 전부 코드 구조와 정합).
- **요구사항 간 모순 없음** — Exclusions 7(`spec.md:198`, boolean 필터 경로 무변경) ↔ REQ-NET-002("`_derive_line_item_states`와 동일한 구성")는 충돌하지 않는다: 코드에서 `refunded_qty` 누적(`:179-184`)과 `trackable` boolean 필터(`:186-191`)가 **별개 블록**임을 실물로 확인했다. REQ-NET-002는 앞의 블록만 참조한다. Exclusions 1(`order_cancelled` 비목표) ↔ REQ-NET-004 ↔ AC-NET-013 삼중 정합. Exclusions 3(상수 무변경) ↔ AC-NET-014 정합. REQ-NET-024(사전 게이트) ↔ REQ-NET-023(7필드 `"0.00"`)은 재계산으로 정합 확인(사전 게이트라야 `null`이 아니라 `"0.00"`이 나온다).
- **경계 AC의 판별력 실재** — AC-NET-004(b)의 근거 검증: 목록 게이트(`serializers.py:283-284`)를 삭제하면 전량 환불 주문에서 `Decimal("0")/Decimal("0")` → `decimal.InvalidOperation` → 500이 되므로 (b)가 실제로 잡는다 ✓. AC-NET-006a의 하한 삭제 변이가 권수 `-1`에서 `== 0` 분기를 통과하지 못해 기본료 `"1.25"`가 부과된다는 R5의 주장도 재계산으로 확인 ✓.
- **성능 불변식 주장의 코드 근거** — `get_has_refund`(`serializers.py:464`)의 `obj.refunds.exists()`는 prefetch된 `RelatedManager` 위에서 `_result_cache`를 읽으므로 추가 쿼리를 내지 않는다. 상세/목록 양쪽 prefetch에 `refunds`가 실재하므로(`views.py:59`, `:269`) 환불 행이 있는 주문에서도 쿼리 수가 8로 유지된다는 REQ-NET-031/032의 전제가 코드로 뒷받침된다. 목록 8의 유도(1 인증 + 1 COUNT + 1 본문 + 4 prefetch + 1 배치 환율)도 `views.py:268-269`의 prefetch 4개와 일치한다.
- **UNVERIFIED 처리 적절** — 96건/61건/2,847건/311행/#37454(`pk=3513`)/rate 1427.11은 원격 공유 DB 스냅샷이며 본 감사로 확인 불가 → **UNVERIFIED 유지**. `spec.md:19`/`:189`, `spec-compact.md:7`, `acceptance.md:54`, `research.md:373`(미체크 유지)이 스냅샷 일자와 재실측 권고를 병기해 처리가 적절하다.

관찰 사항(결함 아님):
- **O1** `acceptance.md:405`의 `Traces:` 줄이 부정문 안에 REQ-NET-033을 포함하므로(“**REQ-NET-033은 이 AC로 검증되지 않는다**”) 기계 파싱 시 오탐이 발생한다. 사람이 읽으면 명확하고 추적표(`spec.md:175`)는 016만 매핑하므로 실질 문제는 없다.
- **O2** `wraps` 누락 시의 실패 경로는 두 가지다. 6개 문자열 게터는 문서가 설명한 대로 repr 문자열로 baseline과 어긋나지만, `get_total_weight_grams`(`serializers.py:591-596`)는 `MagicMock` 객체 자체를 반환하므로 JSON 렌더링 단계에서 예외로 먼저 죽을 수 있다. 어느 경로든 테스트가 실패하므로 "(a)가 `wraps` 누락을 검출한다"는 결론은 유지된다.

---

## 7. 회귀 점검 (Iteration 2 → 3)

| 2차 결함 | 심각도 | 판정 | 독립 검증 근거 |
|---|---|---|---|
| **N1** AC-NET-016의 RED 분류 거짓(6곳) | major (차단) | **RESOLVED** | §3.1 — (a)가 `:644-648`의 baseline 바이트 동일성으로 재작성되어 무수정/수정 후 양쪽에서 통과함을 시뮬레이션으로 확인. 6곳 문구 전부 참으로 전환. `wraps` 누락 검출력은 실행 검증으로 유지 확인(오히려 하드코딩 값 → 살아있는 기준 비교로 강화). 위험 R16 등록 ✓ |
| **N2** AC-NET-005의 M7 판별력 주장 무근거 | minor | **RESOLVED, 단 과교정** | 지적된 거짓 주장은 삭제되었고 그 삭제는 협의의 M7에서 옳다. 그러나 대체 주장("012 단독", "이 픽스처에서 관측되지 않는다")이 재현되지 않는다 → **D1**(방향은 보수적) |
| **N3** AC-NET-016의 M6 2차 방어선 주장 | minor | **RESOLVED** | §3.4(a) — `:629`의 "부수 효과" 단락 삭제 + `:660`에 한계 명시 + `:695` `M6(009,010)`으로 정정, `spec-compact.md:63`과 3곳 정합 |
| **N4** 무수정 집계 산술 불가 | minor | **RESOLVED** | `:686-693`이 AC 단위 12/5=17, 테스트 단위 16/5=21 표로 분리. 감사 독립 시뮬레이션과 일치 |
| **N5** "잔차 0.01인 유일한 픽스처" | minor | **RESOLVED** | `:247`이 "AC-NET-015도 잔차 +0.01"로 정정 + `:588-595`에 015 잔차 단정 신설(재계산 `80.01 − 80.00 = +0.01` ✓). 단 `spec.md:179` 전파 누락 → D3 |
| **N6** `:163` stale 괄호 | minor | **RESOLVED** | `:163`이 "AC-NET-005는 정확 일치 대신 잔차 `0.01` 자체를 직접 단정한다"로 갱신. 015 언급 누락은 D4 |
| **N7** `spec-compact.md` 위험 개수 stale | minor | **RESOLVED** | `:95` "위험 **17건**" = `plan.md` 실측 R1–R17 ✓ |
| **N8** parametrize 열거에 003 누락 | minor | **RESOLVED** | `spec.md:219` "**003**/004/006a/006b" ✓ (17 + 4 = 21 정합) |
| **N9** `_make_order`가 `total_price=None` 불가 | minor | **RESOLVED** | §3.2 — `plan.md:64`의 [HARD] + 우회 형태 + R17. `test_spec_021.py:74` 실물과 `TypeError` 메시지까지 실행 확인 |
| **N10** 잔차 단정 설명 과대 | minor | **RESOLVED(부분)** | `acceptance.md:256`이 "특성화 단정, 상한 준수는 그 귀결"로 축소 ✓. `spec.md:179`는 미전파 → D3 |
| **N11** frontmatter/HISTORY 날짜 불일치 | minor | **RESOLVED** | `spec.md:6` = `:21` = 2026-08-18 ✓ |

**정체(stagnation) 없음.** 3회 연속 동일 결함으로 남은 항목은 0건이다. 1차 D1–D14 14건, 2차 N1–N11 11건 전부 해소되었으며, 이번 라운드의 D1은 "N2 해소 과정에서 반대 방향으로 새로 생긴" 결함이므로 진전 없음이 아니다. 다만 **M7의 판별력 매핑은 세 버전 모두 부정확했다**(v1.0: 005가 잡는다 → 거짓 / v1.2: 012 단독 → 거짓). 판별력 주장을 실제 변이 실행으로 검증하는 습관이 아직 완전히 정착하지 않았다는 신호로 기록한다.

---

## 8. 최종 판정 근거 — 차단 결함인가, 문서 결함인가

**남은 결함 4건 전부 문서 결함이며 `/moai run`을 해치지 않는다.** 근거를 구분해 명시한다.

**구현을 차단하지 않는 근거 (적극적 확인):**

1. **테스트 작성이 막히지 않는다.** 21개 테스트의 픽스처가 전부 완전 지정되어 있고, 헬퍼 3개의 확장 필요성(특히 `total_price=None` 함정)이 [HARD]로 명시되어 있으며, 기대값 약 130개를 감사가 독립 재계산해 **불일치 0건**을 확인했다. 구현자가 문서 값을 그대로 써도 테스트는 정답을 단정한다.
2. **RED 게이트가 실물과 일치한다.** 17개 AC 무수정 시뮬레이션 결과가 5개 문서의 "AC 12/5, 테스트 16/5"와 정확히 일치한다. **2차 N1이 차단이었던 이유는 값이 틀렸기 때문이 아니라 문서가 구현자에게 모순된 지시를 주어 가장 값싼 해소책이 단독 판별자 삭제였기 때문이다.** 그 모순은 소멸했고, 남은 4건 중 어느 것도 구현자에게 상충하는 지시를 주지 않는다.
3. **GREEN 지침이 정확하다.** 감사는 `plan.md:20-47`의 의사코드를 그대로 구현해 17개 AC의 기대값을 재현했다. 계획대로 구현하면 테스트가 통과한다.
4. **오류 방향이 보수적이다.** D1은 커버리지를 **과소** 주장한다. 긍정 판별력 주장 11건(`:695` 커버리지 표)은 전수 검증 결과 **전부 참**이므로 미커버 변이가 0건이고, `spec.md:182`의 [HARD] 무결성 규칙(위반을 검출할 수 없는 AC에 매핑된 REQ = 미커버)에 걸리는 REQ도 0건이다. D1의 실제 효과는 AC-NET-002/012의 픽스처 보존을 필요 이상으로 강하게 요구하는 것뿐이며, 그 지시는 무해하다. D2는 과소 진술(6 vs 7), D3/D4는 커버리지 과소 기재다. **어느 것도 "고쳐도 되는 것을 고치라고 말하지 않는다."**

**그럼에도 감점한 이유:** 이 SPEC은 §0에서 "모든 AC는 자신이 잡는 변이를 명시한다"를, `spec.md:182`에서 "검출할 수 없는 AC에 매핑된 REQ는 미커버"를 스스로 기준으로 세웠다. D1은 그 기준의 **역방향 적용 실패**(잡는데 못 잡는다고 적음)이며, 저자가 광의의 M7을 직접 검토했다고 서술하면서 `margin_amount`를 확인하지 않은 점은 검증 규율의 공백이다. 그래서 Clarity/Testability를 1.0에서 감점했다.

**결론:** Must-Pass 4개 전부 통과(MP-4는 N/A), 2차 차단 결함 N1의 실질 해소를 코드·실행·시뮬레이션 3중으로 확인, 인용 어긋남 0건, 수치 불일치 0건, 추적성 기계 검증 통과. 남은 4건은 구현을 차단하지 않는 문서 정확성 결함이며, 3차 반복이 최종 라운드임을 고려해도 이들을 이유로 SPEC을 반려하는 것은 정당화되지 않는다. **PASS.**

**구현 착수 후 정정을 권고하는 항목(선택, 차단 아님):** ① `acceptance.md:474`/`:695`/`:699`의 단독 판별자를 M10/M11 2건으로 축소하고 M7=(001,012)·M4=(002,012,015)로 정정 — 단 `:699`의 픽스처 보존 지시 4줄은 유지. ② `acceptance.md:216`/`:260`을 "협의의 M7은 미관측, 광의는 `margin_amount`가 `"27.11"`로 갈려 관측"으로 정확화. ③ `acceptance.md:601-604` 표에 `shipping_cost` 열 추가 + `:607` "6개" → "7개". ④ `spec.md:179`에 AC-NET-015를 추가하고 "상한 자체를 검증" → "이 두 픽스처의 잔차를 특성화 단정으로 고정(상한 준수는 그 귀결)"로 축소. ⑤ `acceptance.md:163`에 015의 동일 단정을 병기.

Verdict: PASS
