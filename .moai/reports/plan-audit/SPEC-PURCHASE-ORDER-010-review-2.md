# SPEC Review Report: SPEC-PURCHASE-ORDER-010
Iteration: 2/3
Verdict: FAIL
Overall Score: 0.91 (informational only — Must-Pass Firewall forces overall FAIL regardless of this average)

Reasoning context ignored per M1 Context Isolation. This audit is based solely on `spec.md` at `.moai/specs/SPEC-PURCHASE-ORDER-010/spec.md` (216 lines, read in full) and the prior iteration report at `.moai/reports/plan-audit/SPEC-PURCHASE-ORDER-010-review-1.md`. No `acceptance.md` or `plan.md` exists in the SPEC directory (directory listing shows only `spec.md`), so no further cross-reference was possible.

## Pre-Read Failure Mode Checklist (M2)
Before reading, the following plausible failure modes were assumed present and checked: REQ number gaps/duplicates/inconsistent suffixing; informal or hybrid Given/When/Then language mislabeled as EARS; missing/malformed YAML frontmatter fields; implementation detail (HOW) leaking into REQ text; broken traceability (orphaned REQs/ACs, missing Traces tags); hardcoded language-specific tooling; vague/absent Exclusions; internal contradictions; regression of previously-fixed defects.

## Must-Pass Results
- [PASS] MP-1 REQ number consistency: `REQ-DMG-001` through `REQ-DMG-008` (spec.md:L82,84,86,88,92,94,98,102,106) form a sequential, non-duplicated, consistently 3-digit-zero-padded base series (001–008) with no gaps. Note: `REQ-DMG-005B` (L94) is a letter-suffixed sub-requirement of `REQ-DMG-005` (L92), introduced in this revision to split a previously-bundled requirement (per HISTORY L21). This does not create a gap or duplicate in the base numbering and is documented in HISTORY as an intentional split, so it does not trigger MP-1 failure under the stated rubric text ("no gaps, no duplicates, consistent zero-padding"). Flagged as a minor observation in Defects (N5) for author awareness, not a firewall violation.
- [FAIL] MP-2 EARS format compliance: 4 of 9 acceptance criteria still use a **Given/When/Then hybrid structure** rather than a pure EARS pattern, and 1 more uses non-standard phrasing matching none of the five templates. Evidence:
  - spec.md:L128 (AC-DMG-005): "**Given** a LineItem with `purchase_status=\"damaged_exchange\"` that is already linked to a prior purchase order, **when** any of the four SKU-based reorder-candidate queries is evaluated for that SKU, the system shall include that LineItem in the result set." — this is Given/When/Then test-scenario syntax with a "shall" appended, not the EARS Event-driven template "When [trigger], the [system] shall [response]."
  - spec.md:L130 (AC-DMG-005B): same Given/when/shall hybrid structure.
  - spec.md:L132 (AC-DMG-006): same Given/when/shall hybrid structure, additionally bundling two shall-clauses.
  - spec.md:L134 (AC-DMG-006B): same Given/when/shall hybrid structure.
  - spec.md:L136 (AC-DMG-007): "After the migration is applied, `damaged_exchange` and its corresponding note-type value shall be selectable, and no existing LineItem or LineItemNote record shall have its stored values altered by the migration." — uses "After X, Y shall be Z," which matches none of the five canonical EARS templates (Ubiquitous/Event-driven/State-driven/Optional/Unwanted) verbatim.
  This is precisely the M3 rubric's cited failure mode ("Given/When/Then test scenarios mislabeled as EARS") and the M5 MP-2 definition's explicit exclusion ("mixed informal/formal within a single criterion = FAIL"). **FAIL.**
- [PASS] MP-3 YAML frontmatter validity: All six required fields are now present with correct types — `id: SPEC-PURCHASE-ORDER-010` (L2, string), `version: 1.1.0` (L3, string), `status: draft` (L4, string, valid enum value), `created_at: 2026-08-07` (L6, ISO date string — newly added, fixes iteration-1 D2), `priority: High` (L9, string), `labels: [purchase-order, reorder-queue, backend]` (L11, array — newly added, fixes iteration-1 D2). **PASS.**
- [N/A] MP-4 Section 22 language neutrality: N/A — single-language backend feature (Django/Python `purchase_status` field + TypeScript frontend dropdown), not multi-language tooling content.

**Must-Pass Firewall verdict: MP-2 fails independently. Overall Verdict = FAIL.**

## Category Scores (0.0-1.0, rubric-anchored)
| Dimension | Score | Rubric Band | Evidence |
|-----------|-------|-------------|----------|
| Clarity | 0.75 | 0.75 — minor ambiguity a reasonable engineer would resolve consistently | REQ-DMG-005 was split into REQ-DMG-005/005B (spec.md:L92,94), resolving iteration-1's bundled-requirement ambiguity (D8). Remaining minor issue: the AC-DMG-XXX ID sequence has an unexplained gap (AC-DMG-004 does not exist; AC-DMG-003 at L126 covers both REQ-DMG-003 and REQ-DMG-004) which a reader must infer is intentional rather than a dropped item. |
| Completeness | 1.0 | 1.0 — all required sections present, frontmatter complete, at least one exclusion entry | All frontmatter fields present with correct types (L2-11, see MP-3). All required sections present: HISTORY (L16), WHY-equivalent "문제 정의" (L29), WHAT "솔루션 개요"/"범위" (L33,43), HOW "설계 결정" (L52), REQUIREMENTS (L78), ACCEPTANCE CRITERIA (L118), Exclusions with 3 specific entries (L112-114). |
| Testability | 1.0 | 1.0 — every AC is binary-testable, no weasel words | Grep across the full document for weasel words ("reasonable", "appropriate", "adequate", "정상적으로", "적절", "합리적") found exactly one hit, at spec.md:L156, which is inside the "테스트 시나리오 (BDD, 참고용)" section explicitly marked reference-only (not a formal AC per L120's own scoping statement). All 9 formal AC-DMG-XXX entries (L122-138) state concrete, binary pass/fail conditions (e.g., L124: "the system shall apply it exactly as it would any other valid `purchase_status` value, with no additional error or side effect"). |
| Traceability | 1.0 | 1.0 — every REQ has an AC, every AC references a valid REQ, no orphans | Every AC-DMG-XXX (L122-138) carries an explicit "— Traces: REQ-DMG-XXX." tag. Verified full coverage: REQ-DMG-001→AC-DMG-001(L122), 002→AC-DMG-002(L124), 003+004→AC-DMG-003(L126), 005→AC-DMG-005(L128), 005B→AC-DMG-005B(L130), 006→AC-DMG-006(L132)+AC-DMG-006B(L134), 007→AC-DMG-007(L136), 008→AC-DMG-008(L138). No orphaned AC and no uncovered REQ found (resolves iteration-1 D5/D6/D7). |

## Defects Found

N1. spec.md:L128,130,132,134 (AC-DMG-005, AC-DMG-005B, AC-DMG-006, AC-DMG-006B) — Four acceptance criteria retain a "Given X, when Y, the system shall Z" hybrid structure instead of the pure EARS Event-driven template "When [trigger], the [system] shall [response]." This is the exact anti-pattern the rubric (M3) and firewall (MP-2) explicitly name as a failure case. — Severity: critical

N2. spec.md:L136 (AC-DMG-007) — Uses "After the migration is applied, X and Y shall be Z," which does not match any of the five canonical EARS templates verbatim (not "When", "While", "Where", "If...then", nor a bare "The system shall" ubiquitous statement). — Severity: major

N3. spec.md:L132 (AC-DMG-006) — Bundles two independent assertions into a single AC ("the system shall set that LineItem's `purchase_status` to `unordered`..." and, separately, "...a LineItem with the same status that is NOT part of the current batch shall remain unchanged"), violating single-clause EARS structure and reducing single-responsibility clarity of the criterion. — Severity: minor

N4. spec.md:L118-138 — The AC-DMG-XXX numbering sequence skips "AC-DMG-004" (jumps from AC-DMG-003 at L126 directly to AC-DMG-005 at L128), because AC-DMG-003 traces to both REQ-DMG-003 and REQ-DMG-004. This is not explained inline and could read as a dropped acceptance criterion on first inspection. — Severity: minor

N5. spec.md:L94,134 (REQ-DMG-005B, AC-DMG-006B) — Letter-suffixed IDs (005B, 006B) are introduced without an explicit numbering-convention note in the document itself (the rationale is only in the HISTORY changelog entry, L21). Consider either documenting the sub-lettering convention explicitly near the REQUIREMENTS section header, or renumbering sequentially, for long-term consistency. — Severity: minor

N6. spec.md:L9 — `priority: High` remains capitalized rather than the lowercase enum convention (critical/high/medium/low) implied by the frontmatter rubric. Carried over unresolved from iteration 1's D3 (only the `status` half of D3 was fixed). — Severity: minor

## Chain-of-Verification Pass

Second-look findings: Re-read all eight base REQ-DMG entries (001–008, including the 005B sub-requirement) individually rather than sampling — confirmed all seven "definitions" sections (데이터 모델/읽기측/쓰기측/마이그레이션/하위 호환) map cleanly to their REQ entries with no HOW-level detail (file paths, line numbers, migration filenames) leaking into REQ text, confirming iteration-1 D4 is fully resolved. Re-read all nine AC-DMG entries individually (001, 002, 003, 005, 005B, 006, 006B, 007, 008) rather than stopping after the first few — this individual re-read is what surfaced N1/N2 (the Given/When/shall hybrid persisting in the 005/005B/006/006B/007 cluster after only partially fixing the 001/002/003/008 cluster); a skim-level pass would likely have accepted the "(Event-Driven)"/"(Ubiquitous)" labels at face value and missed that the body text underneath four of them is still BDD-shaped. Verified REQ-to-AC traceability exhaustively (not sampled) for all 8 REQ-DMG entries — confirmed zero orphans and zero uncovered REQs, resolving iteration-1 D5/D6/D7 in full. Re-checked the Exclusions section (spec.md:L110-114) for specificity — all 3 entries remain concrete with stated rationale (SC-6 still passes). Re-scanned for contradictions between REQ-DMG-005/005B/006 (read-side exposure vs. write-side auto-reset) and the Exclusions list — none found; the batch-scoping and explicit-override-precedence rules in REQ-DMG-006 (L98) are consistent with the Exclusions' warehouse-receipt/CS-branch carve-out. Grepped the full document for weasel-word tokens rather than relying on visual scan — confirmed the single hit (L156) falls outside the formal AC section. No additional new defects were found beyond N1–N6 listed above.

## Regression Check (Iteration 2)

Defects from iteration 1 (`.moai/reports/plan-audit/SPEC-PURCHASE-ORDER-010-review-1.md`):

- D1 (critical, spec.md:L110-162 iter-1 — all nine ACs used Given/When/Then BDD format): **UNRESOLVED (partial)**. 5 of 9 ACs were rewritten into compliant or near-compliant EARS form (AC-DMG-001 L122, AC-DMG-002 L124, AC-DMG-003 L126, AC-DMG-008 L138 are compliant). However, AC-DMG-005 (L128), AC-DMG-005B (L130), AC-DMG-006 (L132), AC-DMG-006B (L134) still embed "Given...when...shall" hybrid structure, and AC-DMG-007 (L136) uses non-template phrasing. This is the same root defect recurring in a narrower but still-blocking form — see N1/N2 above. This is the sole reason MP-2 still fails this iteration.
- D2 (critical, spec.md:L1-10 iter-1 — missing `labels`, no `created_at`): **RESOLVED**. `labels` (L11) and `created_at` (L6) both now present with correct types.
- D3 (minor, spec.md:L4,L8 iter-1 — invalid `status: planned`, capitalized `priority: High`): **PARTIALLY RESOLVED**. `status` is now `draft` (valid enum value) — resolved. `priority: High` remains capitalized — unresolved, restated as N6.
- D4 (major, spec.md:L74,88,96 iter-1 — file paths/line numbers/migration filenames embedded in REQ text): **RESOLVED**. Current REQ-DMG-001–008 text (L82-106) contains no file:line references, migration filenames, or field lists; that detail now lives exclusively in the "설계 결정" section (L52-74) and the external `SPEC-ORDER-011/research.md` reference (L25, L66, L70).
- D5 (major, spec.md:L74-96 iter-1 — REQ-DMG-001/002/007 had no dedicated AC): **RESOLVED**. AC-DMG-001 (L122), AC-DMG-002 (L124), AC-DMG-007 (L136) now exist and trace to each respectively.
- D6 (major, spec.md:L158-163 iter-1 — orphaned AC-6 with no REQ backing): **RESOLVED**. A new REQ-DMG-008 (L106) was added specifically to back this criterion, and AC-DMG-008 (L138) traces to it explicitly.
- D7 (major, spec.md:L110-162 iter-1 — no AC carried an explicit REQ trace tag): **RESOLVED**. Every AC-DMG-XXX (L122-138) now carries an explicit "— Traces: REQ-DMG-XXX." tag.
- D8 (minor, spec.md:L84 iter-1 — REQ-DMG-005 bundled two distinct bypass behaviors): **RESOLVED**. Split into REQ-DMG-005 (4-site pattern, L92) and REQ-DMG-005B (ConfirmOrderView exception, L94), each with its own AC.
- D9 (minor, spec.md:L156,L162 iter-1 — weasel words "동일하게 동작한다"/"정상적으로 포함된다" in formal ACs): **RESOLVED**. Those two AC entries no longer exist in their prior form; the one remaining weasel-word occurrence in the whole document (L156) is inside the explicitly out-of-scope BDD reference section, not a formal AC.
- D10 (minor, spec.md:L65,90 iter-1 — unqualified cross-document reference to "REQ-CON-022"): **RESOLVED**. L73 now reads "REQ-CON-022, `SPEC-ORDER-007`에서 정의" — source SPEC is explicitly named.

Stagnation check: D1 is the only defect persisting across both iterations, and it has meaningfully narrowed in scope (9/9 ACs non-compliant → 5/9 ACs non-compliant, with the remaining 4 fully fixed). This does not meet the stagnation-detection bar of "a defect appears in all three iterations unchanged" — there is clear progress on this defect, not a stall. However, since this is iteration 2 of 3, the next iteration is the last opportunity to fully resolve D1 before automatic escalation triggers regardless of other scores.

## Recommendation

This SPEC cannot proceed to Run phase in its current form. manager-spec must address the following before resubmission:

1. **Fix MP-2 (critical, blocking — same root cause as iteration 1, now narrowed to 5 of 9 ACs)**: Rewrite AC-DMG-005, AC-DMG-005B, AC-DMG-006, AC-DMG-006B, and AC-DMG-007 to drop the "Given X, when Y, the system shall Z" preamble and restate as pure EARS Event-driven form. Example for AC-DMG-005 (spec.md:L128): replace "Given a LineItem with `purchase_status=\"damaged_exchange\"` that is already linked to a prior purchase order, when any of the four SKU-based reorder-candidate queries is evaluated for that SKU, the system shall include that LineItem in the result set." with something like "When any of the four SKU-based reorder-candidate queries is evaluated for a SKU that has a LineItem with `purchase_status=\"damaged_exchange\"` already linked to a prior purchase order, the system shall include that LineItem in the result set." Apply the same transformation to AC-DMG-005B, AC-DMG-006, AC-DMG-006B. For AC-DMG-006 (L132), also split the trailing "...and a LineItem NOT part of the current batch shall remain unchanged" clause into its own AC (or fold it into a State-driven/Ubiquitous form) rather than compounding it onto the Event-driven clause (see N3). For AC-DMG-007 (L136), restate using "When the migration is applied, the system shall make `damaged_exchange` and its corresponding note-type value selectable, and shall NOT alter any existing LineItem or LineItemNote record's stored values" to fit the Event-driven template exactly.
2. **Minor cleanup**: Normalize `priority: High` (L9) to lowercase `high` for enum consistency (N6, carried over from iteration 1 D3).
3. **Optional minor cleanups for clarity**: Add a one-line note near the ACCEPTANCE CRITERIA section header explaining that AC-DMG-004 is intentionally absent because AC-DMG-003 covers both REQ-DMG-003 and REQ-DMG-004 (N4); consider documenting the letter-suffix (005B/006B) numbering convention explicitly in the document rather than only in the HISTORY changelog (N5).

Do not resubmit without re-verifying every AC line individually against the five EARS templates listed in this report's M3 rubric — the partial fix pattern in this iteration (fixing the first few ACs while missing the later Event-driven cluster) suggests the rewrite may have been done by editing a subset rather than re-deriving each AC from its EARS template.
